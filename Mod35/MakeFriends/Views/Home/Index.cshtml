@using Microsoft.AspNetCore.Identity
@using MakeFriends.Models.Users
@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager;
@model dynamic

@{
  ViewData["Title"] = "Главная";
}
@if (SignInManager.IsSignedIn(User))
{
    <div>Hello @User.Identity?.Name!</div>
}
else
{
<div class="row">
  <div class="side">
    <div class="center">
      Пользуйтесь нашей самой крутой соцсетью! Регистрируйтесь всего за 2 минуты, делитесь мыслями, находите новых друзей.
    </div>
    <img class="main-img" src="http://pngimg.com/uploads/husky/husky_PNG19.png"  alt="husky"/>
  </div>


  <div class="main">

    <partial name="Login" model="Model" />

    <partial name="Register" model="Model"/>
  </div>

</div>
  
}